import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  media: {
    height: 200,
    width: 200,
    borderRadius: 200,
    margin: '0 auto',
    marginTop: 45,
  },
});